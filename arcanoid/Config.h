#pragma once
#ifndef CONFIG_H
#define CONFIG_H

inline constexpr int WINDOW_WIDTH = 800;
inline constexpr int WINDOW_HEIGHT = 600;

inline constexpr float PADDLE_START_WIDTH = 120.0f;
inline constexpr float PADDLE_HEIGHT = 20.0f;
inline constexpr float PADDLE_SPEED = 600.0f;

inline constexpr float BALL_RADIUS = 10.0f;
inline constexpr float BALL_START_SPEED = 300.0f;

inline constexpr int BLOCK_ROWS = 5;
inline constexpr int BLOCK_COLS = 10;
inline constexpr float BLOCK_HEIGHT = 25.0f;
inline constexpr float BLOCK_PADDING = 5.0f;

#endif
